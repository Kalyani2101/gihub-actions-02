name: CICD

on: 
  workflow_dispatch:
    
jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: clone the code
        run: echo " clonning the code"

  build:
    runs-on: ubuntu-latest
    steps:
      - name: build using docker
        run: echo "code building"

  test:
    runs-on: ubuntu-latest
    steps:
      - name: running testcases
        run: echo " running testcases"

  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Deploying on the machine
      run: echo " deploying the code"
      
      
